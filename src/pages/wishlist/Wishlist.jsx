import React from "react";

const Wishlist = () => {
    return (
        <div className="flex items-center justify-center min-h-screen">
            <h2 className="text-white text-7xl">Sahifa bo'sh</h2>
        </div>
    );
};

export default Wishlist;
